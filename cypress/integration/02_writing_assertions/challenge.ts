beforeEach( () => {

  cy
    .visit(); // ⚠️ add ID of your board

})

// #1: create a new list and assert it is visible
it('creating a list', () => {

})

// #2: create one more list and assert that there are exactly two in the app
it('asserting number of lists', () => {

})

// #3: start this test with a single list in the app. delete it and then assert it does not exist
it('deleting a list', () => {

})

// #4: start the test with a single card, check it and then assert it is checked
it('asseting che checked state', () => {

})

// #5: start test with a single list in the app. change the name of the list and then assert the changed name of the list
it('asserting list name', () => {

})

// #6: create a new card and assert its text
it('assert text of created card', () => {

})